"""Block-sparse crosscoders: unsupervised cross-layer manifold discovery.

Phased experiment (see DESIGN.md). Modules land phase by phase:
Phase 0 blockification, Phase 0.5 cross-layer flow matching, Phase 1 the
BSC trainer + scalar baseline, Phase 2's import bridge lands in saklas.
"""

__version__ = "0.0.1"

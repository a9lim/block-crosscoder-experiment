# Figure catalog — awaiting Phase 0.5

The Phase-0 figure catalog was deleted on 2026-07-20 because its checkpoints,
codec evidence, and several geometry labels failed the adversarial audit.
There is no current winner and no qualified generated figure.

After the Phase-0.5 campaign produces a promoted, hash-bound run, regenerate
the catalog with `bsc refresh-analysis`. The corrected contract uses
activation-weighted contribution and centered used-span spectra, explicit
sample eligibility, and trained shared-code endpoints; decoder capacity is
never labeled as used dimension.
